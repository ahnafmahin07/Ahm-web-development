const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
addEventListener('load',()=>setTimeout(()=>$('#preloader')?.classList.add('hide'),400));
const header=$('#header'),menu=$('#menu'),nav=$('#navlinks');
addEventListener('scroll',()=>{header.classList.toggle('scrolled',scrollY>20);$('#top').classList.toggle('show',scrollY>500)},{passive:true});
menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open);menu.setAttribute('aria-label',open?'Close menu':'Open menu')});
$$('#navlinks a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const ro=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');ro.unobserve(e.target)}}),{threshold:.1});$$('.reveal').forEach(x=>ro.observe(x));
const co=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting)return;const el=e.target,n=+el.dataset.count,start=performance.now();const run=t=>{let p=Math.min((t-start)/1200,1);el.textContent=Math.round(n*(1-Math.pow(1-p,3)))+(n===50?'+':'%');if(p<1)requestAnimationFrame(run)};requestAnimationFrame(run);co.unobserve(el)}),{threshold:.6});$$('[data-count]').forEach(x=>co.observe(x));
$$('.filters button').forEach(b=>b.onclick=()=>{$$('.filters button').forEach(x=>x.classList.remove('active'));b.classList.add('active');const f=b.dataset.filter;$$('.portfolio article').forEach(x=>x.classList.toggle('hide',f!=='all'&&x.dataset.cat!==f))});
const modal=$('#modal');$$('.view').forEach(b=>b.onclick=()=>{$('#modalImg').src=b.dataset.img;$('#modalImg').alt=b.dataset.title;$('#modalTitle').textContent=b.dataset.title;modal.classList.add('open')});$$('[data-close]').forEach(x=>x.onclick=()=>modal.classList.remove('open'));addEventListener('keydown',e=>e.key==='Escape'&&modal.classList.remove('open'));
const slides=$$('.slide'),dots=$('#dots');let idx=0;slides.forEach((_,i)=>{const d=document.createElement('button');d.className='dot'+(!i?' active':'');d.onclick=()=>show(i);dots.appendChild(d)});function show(i){idx=(i+slides.length)%slides.length;slides.forEach((s,j)=>s.classList.toggle('active',j===idx));$$('.dot',dots).forEach((d,j)=>d.classList.toggle('active',j===idx))}$('#prev').onclick=()=>show(idx-1);$('#next').onclick=()=>show(idx+1);let timer=setInterval(()=>show(idx+1),6000);$('#slides').onmouseenter=()=>clearInterval(timer);$('#slides').onmouseleave=()=>timer=setInterval(()=>show(idx+1),6000);
$('#top').onclick=()=>scrollTo({top:0,behavior:'smooth'});
const form=$('#contactForm'),msg=$('#formMessage'),submit=$('#submitBtn'),submitText=$('#submitText');form?.addEventListener('submit',async e=>{e.preventDefault();if(!form.checkValidity()){form.reportValidity();return}submit.disabled=true;submitText.textContent='Sending...';msg.className='';msg.textContent='';try{await emailjs.sendForm('ahmwebdevelopment','template_mmnq9fk',form);msg.className='ok';msg.textContent="✓ Message sent successfully! We'll get back to you soon.";form.reset();submitText.textContent='Message Sent ✓';setTimeout(()=>{submitText.textContent='Send Message';submit.disabled=false},3000)}catch(err){console.error('EmailJS:',err);msg.className='err';msg.textContent='✕ Message could not be sent. Please try again or contact us on WhatsApp.';submitText.textContent='Try Again';submit.disabled=false}});
